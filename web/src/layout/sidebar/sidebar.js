import React, { useContext } from "react";
import SpinnerContext from "../../app/SpinnerContext";
import { useToggle } from "../../utils/hooks";

const Sidebar = () => {
    const { options } = useContext(SpinnerContext);

    
}
